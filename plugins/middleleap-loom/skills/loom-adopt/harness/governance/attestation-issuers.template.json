{
  "_comment": "ADOPT: the allowed-issuers registry — the attestation contract's key-distribution half (Loom 2.0 §6). core/attestations.mjs VERIFIES against this file: only attestations from an issuer listed here, checked by the named mechanism, count as authenticated. The ed25519 mechanism is verified in-process (real crypto, negative-tested); ci-oidc and sigstore mechanisms are verified by the platform in CI — until you wire them and record activation evidence, they report UNVERIFIED-HERE rather than silently passing. Own this file in CODEOWNERS by a non-builder group.",
  "issuers": [
    {
      "id": "bank-ci",
      "mechanism": "ci-oidc",
      "description": "The protected CI runner's OIDC identity — gate evidence envelopes",
      "verify": {
        "kind": "github-attestation",
        "repository_owner": "your-org"
      }
    },
    {
      "id": "bank-approvals",
      "mechanism": "sigstore",
      "description": "SUPERSEDED for human approvals — human approval assertions now live in assertion-issuers.json (Factory Floor WS2 · D2.4), which is deliberately a SEPARATE registry so that a service key can never vouch for a person. Keep this entry only for service-issued envelopes; do not reuse its trust anchor in the assertion registry (the gate rejects a shared anchor).",
      "verify": {
        "kind": "sigstore-identity",
        "issuer": "ADOPT: a SERVICE issuer — it must NOT be the identity provider used in assertion-issuers.json (the gate rejects a shared trust anchor)",
        "subject_pattern": "*@your-org.example"
      }
    },
    {
      "id": "demo-anchor-signer",
      "mechanism": "ed25519",
      "description": "DEMO reference issuer — a real ed25519 key pair; the private key is NOT shipped (regenerate your own; this one signs only the bundled example anchor)",
      "verify": {
        "public_key": "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEAsbjIWdwF4iiTTOfICsqf2J/YWtw4YVRNhXhswb8TAzI=\n-----END PUBLIC KEY-----\n"
      }
    },
    {
      "id": "demo-floor-bridge",
      "mechanism": "ed25519",
      "identity": "svc-floor-bridge",
      "description": "DEMO reference issuer — the transcribing service identity for the bundled approval-attestation example. Proves CUSTODY only: a transcription signature is never approval authority (see governance/assertion-issuers.template.json). Private key NOT shipped.",
      "verify": {
        "public_key": "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEARfrBenT2tIMDHFsRS3kQLgR7lYZsYvsENlhKnQNSpj0=\n-----END PUBLIC KEY-----\n"
      }
    }
  ]
}
