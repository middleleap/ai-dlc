{
  "_comment": "ADOPT: the ASSERTION-issuers registry — identity-provider verification material for human approval assertions (Factory Floor WS2 · D2.4). core/approval-attestations.mjs verifies against this file when a compiled plan requires the `approval-attestation` capability. Own it in CODEOWNERS by a non-builder group.",
  "_why_separate_from_attestation_issuers": "These two registries must never merge. attestation-issuers.json holds SERVICE keys — CI runners, sync workers, evidence signers — which prove custody: that a machine handled a record faithfully. This file holds IDENTITY-PROVIDER material, which proves a named human decided. A record signed only by a service key authenticates the worker, not the approver, so the gate REJECTS any approval assertion whose issuer appears in the service registry. Keeping the namespaces apart is what makes that check possible.",
  "issuers": [
    {
      "id": "bank-idp",
      "mechanism": "oidc-step-up",
      "description": "ADOPT: the institution's identity provider, used for step-up re-authentication at the moment of decision. The assertion must bind a nonce equal to the decision payload digest (see ADR-0005). Verified against pinned IdP metadata by the platform — UNVERIFIED-HERE until you wire it and record activation evidence.",
      "verify": {
        "kind": "oidc",
        "issuer": "https://your-idp.example/oidc",
        "audience": "ADOPT: the client id the approval surface authenticates against",
        "required_acr": "ADOPT: the step-up assurance level your policy demands",
        "subject_claim": "sub",
        "jwks_uri": "ADOPT: pin the JWKS or the keys themselves — an unpinned provider is not trusted"
      }
    },
    {
      "id": "bank-approvals-sigstore",
      "mechanism": "sigstore",
      "description": "ADOPT (optional): short-lived identity certificates binding a human subject to the decision payload. The strategic direction where the institution already runs this infrastructure (ADR-0005, Option C). UNVERIFIED-HERE until wired.",
      "verify": {
        "kind": "sigstore-identity",
        "issuer": "ADOPT: your identity provider — it must NOT share a trust anchor with any entry in attestation-issuers.json",
        "subject_pattern": "*@your-org.example"
      }
    },
    {
      "id": "demo-assertion-signer",
      "mechanism": "ed25519",
      "description": "DEMO reference issuer — a real ed25519 key pair standing in for an identity provider so the contract is exercised end to end in the bundle. The private key is NOT shipped. Do not carry this entry into a real registry: replace it with your provider before the capability is compiled for any change.",
      "verify": {
        "public_key": "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEATAsISc/hw6zXXJzoUOl+rTrtt60nVfWUo/Ksx+M9sE4=\n-----END PUBLIC KEY-----\n"
      }
    }
  ]
}
