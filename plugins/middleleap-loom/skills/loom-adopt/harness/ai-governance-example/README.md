# AI governance worked fixture

This synthetic bundle makes the 2.4 AI route executable in adoption CI. The workflow mounts its
records into an adopted repository and exercises AI governance, fairness and contestability
against one implicated model role.

It is not a bank policy, production population, model validation, legal interpretation or runtime
observation. Replace every record, owner, threshold, evidence artifact and operational path with
institution-owned material before using the route for a real product.
