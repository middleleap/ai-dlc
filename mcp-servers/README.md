# MCP Servers

Model Context Protocol server configurations — ready-to-use MCP servers for connecting AI agents to external services and data sources.

## Adding an MCP Server

1. Create a folder: `mcp-servers/<category>/<server-name>/`
2. Include a `SERVER.md` with YAML frontmatter (`name`, `description`)
3. Register in `marketplace.json` at the repo root
4. See [CONTRIBUTING.md](../CONTRIBUTING.md) for naming conventions and size guidelines

No MCP servers have been published yet. Be the first!
