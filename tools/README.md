# Tools

Standalone tools that AI agents can invoke — calculators, validators, formatters, converters, and utilities.

## Adding a Tool

1. Create a folder: `tools/<category>/<tool-name>/`
2. Include a `TOOL.md` with YAML frontmatter (`name`, `description`)
3. Register in `marketplace.json` at the repo root
4. See [CONTRIBUTING.md](../CONTRIBUTING.md) for naming conventions and size guidelines

No tools have been published yet. Be the first!
